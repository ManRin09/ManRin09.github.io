import { initNavigation } from './nav.js';

document.addEventListener('DOMContentLoaded', () => {
  // Initialize the SPA navigation module
  initNavigation();
  
  // Future modules can be initialized here
});
